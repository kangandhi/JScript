let mix = ["Fan", "Camera", 34, null, true];
// array method
console.log(mix.length);
mix.pop();
console.log(mix);

mix.push('kanchan')
console.log(mix);

mix.shift()
console.log(mix);

const newMix = mix.unshift('kanchan')
console.log(newMix);
console.log(mix);

