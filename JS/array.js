var array = [1, 2, 3, 4, 5, 6, 7];
console.log(array);
for (var i = 0; i < array.length; array++) {
    console.log('array[i]');
}

// array can be iterate with forEach loop (short cut)
// array.forEach(function(element) {
//     console.log(element);
// })

